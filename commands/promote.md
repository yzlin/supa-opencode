---
description: Promote project instincts to global scope
agent: build
---

# Promote Command

Promote instincts in continuous-learning: $ARGUMENTS

## Your Task

Run:

```bash
python3 "${OPENCODE_PLUGIN_DIR}/skills/continuous-learning/scripts/instinct-cli.py" promote $ARGUMENTS
```

If `OPENCODE_PLUGIN_DIR` is unavailable (plugin not loaded), find the script:

```bash
python3 ~/.config/opencode/node_modules/supa-opencode/skills/continuous-learning/scripts/instinct-cli.py promote $ARGUMENTS
```
